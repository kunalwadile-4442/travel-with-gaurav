# Email Configuration (SMTP)
# Use these for sending contact form and popup form emails

# SMTP Host (e.g., smtp.gmail.com, smtp.sendgrid.net)
SMTP_HOST=

# SMTP Port (587 for TLS, 465 for SSL)
SMTP_PORT=587

# Set to "true" for port 465 (SSL)
SMTP_SECURE=false

# SMTP credentials
SMTP_USER=
SMTP_PASS=

# Email where contact form submissions will be sent
# If not set, uses SMTP_USER
CONTACT_EMAIL=
